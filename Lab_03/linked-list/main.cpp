#include "linked-list.h"
int main() {
  LinkedList linkedList();
  linkedList.insert(12);
  linkedList.insert(34);
  linkedList.insert(14);
  // print it
}

// command to compile g++ -std=c++11 <other files>
